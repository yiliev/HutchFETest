import { DataVisualisationLabelPipe } from './data-visualisation-label.pipe';

describe('DataVisualisationLabelPipe', () => {
  it('create an instance', () => {
    const pipe = new DataVisualisationLabelPipe();
    expect(pipe).toBeTruthy();
  });
});
